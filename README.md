# work in progress

# Overview
```
metagross = meta + gross = anti meta
```

tired of the meta? here's a calculator.

right now it only considers type advantages, but if people care enough, i, or someone else through a PR, can add more features.


# Contributing
directory structure
- `./research`: contains the jupyter notebook i used to develop the main algorithms
- `./app`: UI entirely agent generated by spec coding with kiro/claude
- `./.github`: automation to deploy nextjs build to deployment target (which right now is just another github repo using github pages)